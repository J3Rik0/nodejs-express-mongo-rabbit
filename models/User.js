const config = require('../db');

const User = {
  /**
   * Get all users
   * @return {Array}
   */

  all: () => {
    // get all users
  },

  /**
   * Get user by id
   * @param  {int} id
   * @return {Object}
   */
  id: (id) => {
    // find user by id
  }
}
